# Forzar registry público para este proyecto.
# El registry global del usuario (~/.npmrc) apunta al artifactory de Fury
# que bloquea con 403 los paquetes necesarios para este proyecto académico.
# IMPORTANTE: requiere DNS público accesible (no DNS corporativo de MELI).
registry=https://registry.npmjs.org/
